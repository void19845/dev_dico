export const typescript = {
    color: '#3178C6',
    nodes: [
        { id: 'TS', group: 'root', label: 'TypeScript', size: 30, desc: 'A strongly typed superset of JavaScript that adds optional static typing to the language, allowing developers to catch errors early and document code intent.' },
        { id: 'Types', group: 'cat', label: 'Types', size: 20, desc: 'The core system for defining data shapes. In TypeScript, types act as a contract that the compiler enforces during development.' },
        { id: 'Generics', group: 'cat', label: 'Generics', size: 20, desc: 'A tool for creating reusable components that work with a variety of types rather than a single one.' },
        { id: 'Utility', group: 'cat', label: 'Utility Types', size: 20, desc: 'Built-in global types that allow you to transform an existing type into a new one without manual re-definition.' },
        { id: 'Async', group: 'cat', label: 'Asynchrony', size: 20, desc: 'Patterns for handling non-blocking operations, ensuring the main thread remains responsive.' },
        { id: 'Control', group: 'cat', label: 'Control Flow', size: 20, desc: 'Logic for narrowing types and directing program execution based on state.' },
        { id: 'Advanced', group: 'cat', label: 'Advanced Types', size: 20, desc: 'Powerful type-level programming tools for building highly flexible and dynamic type systems.' },
        { id: 'type', group: 'key', label: 'type', size: 12, desc: 'Defines a type alias. It is used to give a new name to a type, which can be a primitive, union, or intersection.', parent: 'Types', params: ['AliasName', 'TypeDefinition'], example: 'type UserID = string | number;', tip: 'Use type aliases for unions and intersections, as interfaces cannot do this directly.' },
        { id: 'interface', group: 'key', label: 'interface', size: 12, desc: 'Defines the structure of an object. Unlike type aliases, interfaces are extendable and ideal for defining APIs or public contracts.', parent: 'Types', params: ['InterfaceName', 'Members'], example: 'interface User { name: string; age: number; }' },
        { id: 'enum', group: 'key', label: 'enum', size: 12, desc: 'A way to define a set of named constants. Enums improve readability by replacing magic numbers or strings with semantic names.', parent: 'Types', params: ['EnumName', 'Members'], example: 'enum Role { Admin, User, Guest }' },
        { id: 'unknown', group: 'key', label: 'unknown', size: 12, desc: 'The safest version of "any". It tells TypeScript that you don\'t know the type yet, and you must perform a type check before using it.', parent: 'Types', params: [], example: 'let value: unknown = "Hello";', tip: 'Always prefer unknown over any when you truly don\'t know the type; it forces you to handle the data safely.' },
        { id: 'never', group: 'key', label: 'never', size: 12, desc: 'Represents a value that should never exist, such as a function that always throws an error or an infinite loop.', parent: 'Types', params: [], example: 'function throwError(): never { throw new Error(); }' },
        { id: 'void', group: 'key', label: 'void', size: 12, desc: 'Used as the return type of functions that do not return a value. It indicates the function performs an action but returns nothing.', parent: 'Types', params: [], example: 'function log(msg: string): void { console.log(msg); }' },
        { id: 'T', group: 'key', label: '<T>', size: 12, desc: 'A type parameter used in generics. It allows a function or class to work with any type provided by the caller.', parent: 'Generics', params: ['T: Constraint'], example: 'function wrap<T>(item: T): T[] { return [item]; }' },
        { id: 'K', group: 'key', label: '<K>', size: 12, desc: 'Typically used in generics to represent a Key. Often constrained using "extends keyof T" to ensure the key exists on a specific object.', parent: 'Generics', params: ['K extends keyof T'], example: 'function getProp<T, K extends keyof T>(obj: T, key: K) { return obj[key]; }' },
        { id: 'Partial', group: 'key', label: 'Partial<T>', size: 12, desc: 'Creates a type where every property of T is made optional. Perfect for "update" functions where only some fields are changed.', parent: 'Utility', params: ['T'], example: 'type UpdateUser = Partial<User>;' },
        { id: 'Readonly', group: 'key', label: 'Readonly<T>', size: 12, desc: 'Makes all properties of T immutable. Any attempt to change a property after creation will cause a compile-time error.', parent: 'Utility', params: ['T'], example: 'const config: Readonly<Config> = { port: 8080 };' },
        { id: 'Pick', group: 'key', label: 'Pick<T, K>', size: 12, desc: 'Creates a new type by selecting a specific subset of properties K from type T.', parent: 'Utility', params: ['T', 'K'], example: 'type UserPreview = Pick<User, "name" | "avatar">;' },
        { id: 'Omit', group: 'key', label: 'Omit<T, K>', size: 12, desc: 'The opposite of Pick. It removes specified properties K from type T, keeping everything else.', parent: 'Utility', params: ['T', 'K'], example: 'type UserWithoutPassword = Omit<User, "password">;' },
        { id: 'Record', group: 'key', label: 'Record<K, T>', size: 12, desc: 'Constructs an object type where keys are of type K and values are of type T. Ideal for maps or dictionaries.', parent: 'Utility', params: ['K', 'T'], example: 'const users: Record<string, User> = { "1": user1 };' },
        { id: 'async', group: 'key', label: 'async', size: 12, desc: 'Marks a function as asynchronous, meaning it will always return a Promise and allows the use of the "await" keyword inside.', parent: 'Async', params: ['function'], example: 'async function fetchData() { ... }' },
        { id: 'await', group: 'key', label: 'await', size: 12, desc: 'Pauses the execution of an async function until a Promise is resolved, returning the resolved value.', parent: 'Async', params: ['Promise'], example: 'const data = await response.json();' },
        { id: 'Promise', group: 'key', label: 'Promise<T>', size: 12, desc: 'An object representing the eventual completion (or failure) of an asynchronous operation and its resulting value.', parent: 'Async', params: ['T'], example: 'const p: Promise<string> = fetchUser();' },
        { id: 'as', group: 'key', label: 'as', size: 12, desc: 'A type assertion that tells the compiler "trust me, I know the type is actually X". Use with caution as it bypasses some checks.', parent: 'Control', params: ['Type'], example: 'const input = document.getElementById("in") as HTMLInputElement;' },
        { id: 'is', group: 'key', label: 'is', size: 12, desc: 'A type predicate used in functions to tell TypeScript that if the function returns true, the variable is of a specific type.', parent: 'Control', params: ['Type'], example: 'function isUser(obj: any): obj is User { return "name" in obj; }' },
        { id: 'cond', group: 'key', label: 'Conditional Types', size: 12, desc: 'Logic that allows a type to change based on a condition, similar to a ternary operator for types.', parent: 'Advanced', params: ['T extends U ? X : Y'], example: 'type IsString<T> = T extends string ? true : false;' },
        { id: 'mapped', group: 'key', label: 'Mapped Types', size: 12, desc: 'Allows you to create a new type by iterating over the keys of an existing type and transforming them.', parent: 'Advanced', params: ['[K in keyof T]: Value'], example: 'type Options<T> = { [K in keyof T]: boolean };' },
        { id: 'template', group: 'key', label: 'Template Literals', size: 12, desc: 'Combines string literals with other types to create highly specific string types.', parent: 'Advanced', params: ['`${T}`'], example: 'type Greeting = `Hello ${string}`;' },
    ],
    links: []
};
