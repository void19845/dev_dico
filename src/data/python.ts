import { LanguageData } from '../types.js';

export const python: LanguageData = {
    color: '#3776AB',
    nodes: [
        { id: 'Py', group: 'root', label: 'Python', size: 30, desc: 'A high-level, interpreted language known for its readability and vast ecosystem of libraries for AI, Data Science, and Web.' },
        { id: 'Data', group: 'cat', label: 'Data Structures', size: 20, desc: 'Python\'s powerful built-in containers for organizing and storing data.' },
        { id: 'Control', group: 'cat', label: 'Control Flow', size: 20, desc: 'The structures used to manage logic, loops, and conditional execution.' },
        { id: 'FP', group: 'cat', label: 'Functional Tools', size: 20, desc: 'Tools that allow you to treat functions as first-class citizens, enabling concise and powerful data processing.' },
        { id: 'OO', group: 'cat', label: 'Object Oriented', size: 20, desc: 'The system for creating blueprints (classes) and instances (objects) to model real-world entities.' },
        { id: 'Magic', group: 'cat', label: 'Magic Methods', size: 20, desc: 'Special "dunder" (double underscore) methods that allow you to customize how objects behave with built-in operators.' },
        { id: 'Context', group: 'cat', label: 'Context Management', size: 20, desc: 'The "with" statement pattern used to ensure resources like files or sockets are closed correctly.' },
        { id: 'AsyncPy', group: 'cat', label: 'Asynchronous', size: 20, desc: 'Modern Python concurrency using the asyncio loop to handle thousands of simultaneous tasks.' },
        { id: 'list', group: 'key', label: 'list()', size: 12, desc: 'Creates a mutable, ordered sequence of elements. The most versatile data structure in Python.', parent: 'Data', params: ['iterable'], example: 'my_list = list("hello") # ["h", "e", "l", "l", "o"]' },
        { id: 'dict', group: 'key', label: 'dict()', size: 12, desc: 'Creates a collection of key-value pairs. Lookups are extremely fast (O(1) average time complexity).', parent: 'Data', params: ['mapping', '**kwargs'], example: 'my_dict = dict(name="Dev", age=25)' },
        { id: 'set', group: 'key', label: 'set()', size: 12, desc: 'An unordered collection of unique elements. Ideal for membership testing and removing duplicates.', parent: 'Data', params: ['iterable'], example: 'my_set = set([1, 2, 2, 3]) # {1, 2, 3}' },
        { id: 'if', group: 'key', label: 'if/elif/else', size: 12, desc: 'Evaluates a boolean expression. Python uses indentation to define the block of code to execute.', parent: 'Control', params: ['expression'], example: 'if x > 0:\\n    print("Positive")' },
        { id: 'for', group: 'key', label: 'for/in', size: 12, desc: 'Iterates over any iterable (list, string, range). Python\'s for loop is actually a "foreach" loop.', parent: 'Control', params: ['item', 'iterable'], example: 'for i in range(5):\\n    print(i)' },
        { id: 'while', group: 'key', label: 'while', size: 12, desc: 'Repeats a block as long as the condition is true. Use with "break" to exit early.', parent: 'Control', params: ['expression'], example: 'while count < 10:\\n    count += 1' },
        { id: 'lambda', group: 'key', label: 'lambda', size: 12, desc: 'A small, anonymous function defined in one line. Used frequently inside map() or filter().', parent: 'FP', params: ['args', 'expression'], example: 'square = lambda x: x*x' },
        { id: 'map', group: 'key', label: 'map()', size: 12, desc: 'Applies a function to every item in an iterable and returns a map object (iterator).', parent: 'FP', params: ['function', 'iterable'], example: 'list(map(str.upper, ["a", "b"]))' },
        { id: 'filter', group: 'key', label: 'filter()', size: 12, desc: 'Creates an iterator of elements for which the provided function returns True.', parent: 'FP', params: ['function', 'iterable'], example: 'list(filter(lambda x: x>0, [-1, 1]))' },
        { id: 'enumerate', group: 'key', label: 'enumerate()', size: 12, desc: 'Allows you to loop through a list while keeping track of the current index automatically.', parent: 'FP', params: ['iterable', 'start'], example: 'for i, val in enumerate(["a", "b"]):' },
        { id: 'zip', group: 'key', label: 'zip()', size: 12, desc: 'Pairs elements from multiple iterables together. Stops when the shortest iterable is exhausted.', parent: 'FP', params: ['*iterables'], example: 'list(zip([1,2], ["a", "b"])) # [(1,"a"), (2,"b")]' },
        { id: 'any', group: 'key', label: 'any()', size: 12, desc: 'Returns True if at least one element in the iterable is truthy.', parent: 'FP', params: ['iterable'], example: 'any([False, True, False]) # True' },
        { id: 'all', group: 'key', label: 'all()', size: 12, desc: 'Returns True only if every single element in the iterable is truthy.', parent: 'FP', params: ['iterable'], example: 'all([True, True, True]) # True' },
        { id: 'sorted', group: 'key', label: 'sorted()', size: 12, desc: 'Returns a new sorted list. The original iterable remains unchanged.', parent: 'FP', params: ['iterable', 'key', 'reverse'], example: 'sorted([3, 1, 2]) # [1, 2, 3]' },
        { id: 'class', group: 'key', label: 'class', size: 12, desc: 'Defines a new object type. Python supports multiple inheritance and dynamic attribute addition.', parent: 'OO', params: ['ClassName', 'Bases'], example: 'class User:\\n    def __init__(self, name): self.name = name' },
        { id: 'self', group: 'key', label: 'self', size: 12, desc: 'The first parameter of any instance method, representing the specific object being operated on.', parent: 'OO', params: [], example: 'def greet(self):\\n    print(f"Hello {self.name}")' },
        { id: 'init', group: 'key', label: '__init__', size: 12, desc: 'The constructor method. It is called automatically when a new object is created.', parent: 'OO', params: ['self', 'args'], example: 'def __init__(self, name): self.name = name' },
        { id: 'str', group: 'key', label: '__str__', size: 12, desc: 'Customizes what happens when you call print(obj) or str(obj).', parent: 'Magic', params: ['self'], example: 'def __str__(self): return f"User({self.name})"' },
        { id: 'repr', group: 'key', label: '__repr__', size: 12, desc: 'Used for debugging. Should return a string that could ideally recreate the object.', parent: 'Magic', params: ['self'], example: 'def __repr__(self): return f"User(id={self.id})"' },
        { id: 'getitem', group: 'key', label: '__getitem__', size: 12, desc: 'Allows an object to be indexed like a list or dict using square brackets [].', parent: 'Magic', params: ['self', 'key'], example: 'def __getitem__(self, key): return self.data[key]' },
        { id: 'with', group: 'key', label: 'with', size: 12, desc: 'A context manager that ensures setup and cleanup (like closing a file) happen automatically.', parent: 'Context', params: ['expression', 'as target'], example: 'with open("file.txt") as f:\\n    content = f.read()' },
        { id: 'async_def', group: 'key', label: 'async def', size: 12, desc: 'Defines a coroutine. It can be paused and resumed, allowing for high-performance I/O.', parent: 'AsyncPy', params: ['name', 'args'], example: 'async def fetch_data():\\n    pass' },
        { id: 'await_py', group: 'key', label: 'await', size: 12, desc: 'Pauses the coroutine until the awaited task completes, without blocking the whole program.', parent: 'AsyncPy', params: ['awaitable'], example: 'result = await fetch_data()' },
        { id: 'yield', group: 'key', label: 'yield', size: 12, desc: 'Produces a value and pauses the function, remembering its state for the next call.', parent: 'FP', params: ['value'], example: 'def count():\n    yield 1\n    yield 2' },
        { id: 'Builtins', group: 'cat', label: 'Built-in Functions', size: 20, desc: 'Core functions available globally without needing an import, covering type conversion, introspection, and I/O.' },
        { id: 'Decorators', group: 'cat', label: 'Decorators', size: 20, desc: 'Functions that wrap other functions or methods to extend their behavior without modifying their source code.' },
        { id: 'ErrorHandling', group: 'cat', label: 'Error Handling', size: 20, desc: 'The exception system used to detect, raise, and gracefully recover from runtime errors.' },
        { id: 'Modules', group: 'cat', label: 'Modules & Imports', size: 20, desc: 'The system for organizing code into reusable files and packages.' },
        { id: 'FileIO', group: 'cat', label: 'File I/O', size: 20, desc: 'Built-in tools for reading from and writing to files on disk.' },
        { id: 'Iterators', group: 'cat', label: 'Iterators', size: 20, desc: 'The protocol that powers for loops, letting objects produce a sequence of values one at a time.' },
        { id: 'TypeHints', group: 'cat', label: 'Type Hints', size: 20, desc: 'Optional static typing annotations that document intent and let tools like mypy catch bugs before runtime.' },
        { id: 'Comprehensions', group: 'cat', label: 'Comprehensions', size: 20, desc: 'Concise syntax for building lists, dicts, sets, and generators from existing iterables in a single line.' },
        { id: 'StdLib', group: 'cat', label: 'Standard Library', size: 20, desc: 'Frequently used modules that ship with Python and cover common tasks like counting, dates, and text patterns.' },
        { id: 'Strings', group: 'cat', label: 'Strings', size: 20, desc: 'Python\'s immutable text type, along with the methods used to inspect, split, and transform it.' },
        { id: 'len_fn', group: 'key', label: 'len()', size: 12, desc: 'Returns the number of items in a container such as a list, string, or dictionary.', parent: 'Builtins', params: ['object'], example: 'len("hello") # 5' },
        { id: 'range_fn', group: 'key', label: 'range()', size: 12, desc: 'Generates an immutable sequence of numbers, commonly used to control the number of iterations in a for loop.', parent: 'Builtins', params: ['start', 'stop', 'step'], example: 'list(range(2, 10, 2)) # [2, 4, 6, 8]' },
        { id: 'print_fn', group: 'key', label: 'print()', size: 12, desc: 'Writes text to standard output, with optional separators and a custom end character.', parent: 'Builtins', params: ['*objects', 'sep', 'end'], example: 'print("a", "b", sep="-") # a-b' },
        { id: 'input_fn', group: 'key', label: 'input()', size: 12, desc: 'Reads a line of text typed by the user and returns it as a string.', parent: 'Builtins', params: ['prompt'], example: 'name = input("Your name: ")' },
        { id: 'type_fn', group: 'key', label: 'type()', size: 12, desc: 'Returns the type of an object, or dynamically creates a new type when given three arguments.', parent: 'Builtins', params: ['object'], example: 'type(42) # <class \'int\'>' },
        { id: 'isinstance_fn', group: 'key', label: 'isinstance()', size: 12, desc: 'Checks whether an object is an instance of a class or a tuple of classes.', parent: 'Builtins', params: ['object', 'classinfo'], example: 'isinstance(42, (int, float)) # True' },
        { id: 'min_fn', group: 'key', label: 'min()', size: 12, desc: 'Returns the smallest item in an iterable or the smallest of several arguments.', parent: 'Builtins', params: ['iterable', 'key'], example: 'min([3, 1, 2]) # 1' },
        { id: 'max_fn', group: 'key', label: 'max()', size: 12, desc: 'Returns the largest item in an iterable or the largest of several arguments.', parent: 'Builtins', params: ['iterable', 'key'], example: 'max([3, 1, 2]) # 3' },
        { id: 'sum_fn', group: 'key', label: 'sum()', size: 12, desc: 'Adds every item in an iterable together, starting from an optional initial value.', parent: 'Builtins', params: ['iterable', 'start'], example: 'sum([1, 2, 3]) # 6' },
        { id: 'abs_fn', group: 'key', label: 'abs()', size: 12, desc: 'Returns the absolute (non-negative) value of a number.', parent: 'Builtins', params: ['number'], example: 'abs(-7) # 7' },
        { id: 'round_fn', group: 'key', label: 'round()', size: 12, desc: 'Rounds a number to a given number of decimal places, using banker\'s rounding on ties.', parent: 'Builtins', params: ['number', 'ndigits'], example: 'round(3.14159, 2) # 3.14' },
        { id: 'id_fn', group: 'key', label: 'id()', size: 12, desc: 'Returns a unique integer identifying an object for its lifetime, corresponding to its memory address in CPython.', parent: 'Builtins', params: ['object'], example: 'id(my_var)' },
        { id: 'super_fn', group: 'key', label: 'super()', size: 12, desc: 'Returns a proxy object that lets you call methods from a parent class, commonly used in __init__ to extend base behavior.', parent: 'OO', params: [], example: 'class Dog(Animal):\n    def __init__(self, name):\n        super().__init__(name)' },
        { id: 'staticmethod_dec', group: 'key', label: '@staticmethod', size: 12, desc: 'Marks a method that does not access the instance or class, behaving like a plain function namespaced inside the class.', parent: 'Decorators', params: [], example: 'class Math:\n    @staticmethod\n    def add(a, b): return a + b' },
        { id: 'classmethod_dec', group: 'key', label: '@classmethod', size: 12, desc: 'Marks a method that receives the class itself (cls) as its first argument instead of an instance.', parent: 'Decorators', params: [], example: 'class User:\n    @classmethod\n    def from_dict(cls, d): return cls(**d)' },
        { id: 'property_dec', group: 'key', label: '@property', size: 12, desc: 'Turns a method into a read-only attribute, allowing computed values to be accessed without parentheses.', parent: 'Decorators', params: [], example: 'class Circle:\n    @property\n    def area(self): return 3.14 * self.r ** 2' },
        { id: 'dataclass_dec', group: 'key', label: '@dataclass', size: 12, desc: 'Automatically generates __init__, __repr__, and __eq__ for a class based on its type-annotated fields.', parent: 'Decorators', params: [], example: '@dataclass\nclass Point:\n    x: int\n    y: int' },
        { id: 'lru_cache_dec', group: 'key', label: '@lru_cache', size: 12, desc: 'Caches a function\'s return values so repeated calls with the same arguments skip recomputation.', parent: 'Decorators', params: ['maxsize'], example: 'from functools import lru_cache\n@lru_cache(maxsize=None)\ndef fib(n): ...' },
        { id: 'custom_dec', group: 'key', label: 'Custom Decorator', size: 12, desc: 'A function that takes another function and returns a wrapped version of it, adding behavior before or after the call.', parent: 'Decorators', params: ['func'], example: 'def logger(func):\n    def wrapper(*a, **kw):\n        print("calling")\n        return func(*a, **kw)\n    return wrapper' },
        { id: 'eq_magic', group: 'key', label: '__eq__', size: 12, desc: 'Customizes what happens when two objects are compared with ==.', parent: 'Magic', params: ['self', 'other'], example: 'def __eq__(self, other): return self.id == other.id' },
        { id: 'len_magic', group: 'key', label: '__len__', size: 12, desc: 'Defines the behavior of the built-in len() function for instances of the class.', parent: 'Magic', params: ['self'], example: 'def __len__(self): return len(self.items)' },
        { id: 'iter_magic', group: 'key', label: '__iter__', size: 12, desc: 'Makes an object iterable by returning an iterator, enabling it to be used in a for loop.', parent: 'Magic', params: ['self'], example: 'def __iter__(self): return iter(self.items)' },
        { id: 'call_magic', group: 'key', label: '__call__', size: 12, desc: 'Allows an instance of a class to be called like a regular function using parentheses.', parent: 'Magic', params: ['self', 'args'], example: 'def __call__(self, x): return x * self.factor' },
        { id: 'add_magic', group: 'key', label: '__add__', size: 12, desc: 'Customizes the behavior of the + operator between an instance and another object.', parent: 'Magic', params: ['self', 'other'], example: 'def __add__(self, other): return Vector(self.x + other.x, self.y + other.y)' },
        { id: 'enter_magic', group: 'key', label: '__enter__', size: 12, desc: 'Runs setup logic when an object is used with the "with" statement, returning the value bound to "as".', parent: 'Magic', params: ['self'], example: 'def __enter__(self): return self' },
        { id: 'exit_magic', group: 'key', label: '__exit__', size: 12, desc: 'Runs cleanup logic when a "with" block ends, even if an exception occurred inside it.', parent: 'Magic', params: ['self', 'exc_type', 'exc_val', 'exc_tb'], example: 'def __exit__(self, *args): self.file.close()' },
        { id: 'break_py', group: 'key', label: 'break', size: 12, desc: 'Immediately exits the nearest enclosing for or while loop.', parent: 'Control', params: [], example: 'for i in range(10):\n    if i == 5:\n        break' },
        { id: 'continue_py', group: 'key', label: 'continue', size: 12, desc: 'Skips the rest of the current loop iteration and moves to the next one.', parent: 'Control', params: [], example: 'for i in range(10):\n    if i % 2 == 0:\n        continue' },
        { id: 'pass_py', group: 'key', label: 'pass', size: 12, desc: 'A null operation used as a placeholder where syntax requires a statement but no action is needed.', parent: 'Control', params: [], example: 'def todo():\n    pass' },
        { id: 'ternary_py', group: 'key', label: 'Ternary Expression', size: 12, desc: 'A compact way to choose between two values based on a condition, written on a single line.', parent: 'Control', params: ['value_if_true', 'condition', 'value_if_false'], example: 'status = "adult" if age >= 18 else "minor"' },
        { id: 'walrus_py', group: 'key', label: ':= (walrus)', size: 12, desc: 'Assigns a value to a variable as part of a larger expression, avoiding a separate assignment line.', parent: 'Control', params: ['name', 'expression'], example: 'if (n := len(data)) > 10:\n    print(f"Too long: {n}")' },
        { id: 'match_py', group: 'key', label: 'match/case', size: 12, desc: 'Structural pattern matching introduced in Python 3.10, similar to a more powerful switch statement.', parent: 'Control', params: ['subject'], example: 'match command:\n    case "start":\n        run()\n    case _:\n        pass' },
        { id: 'assert_py', group: 'key', label: 'assert', size: 12, desc: 'Raises an AssertionError if a condition is false, mainly used for debugging and sanity checks.', parent: 'Control', params: ['condition', 'message'], example: 'assert age >= 0, "Age cannot be negative"' },
        { id: 'global_py', group: 'key', label: 'global', size: 12, desc: 'Declares that a name inside a function refers to a variable in the module-level scope rather than a local one.', parent: 'Control', params: ['name'], example: 'def increment():\n    global counter\n    counter += 1' },
        { id: 'nonlocal_py', group: 'key', label: 'nonlocal', size: 12, desc: 'Declares that a name inside a nested function refers to a variable in the enclosing (but not global) scope.', parent: 'Control', params: ['name'], example: 'def outer():\n    x = 0\n    def inner():\n        nonlocal x\n        x += 1' },
        { id: 'reduce_fn', group: 'key', label: 'reduce()', size: 12, desc: 'Cumulatively applies a function to the items of an iterable, reducing it to a single value.', parent: 'FP', params: ['function', 'iterable', 'initializer'], example: 'from functools import reduce\nreduce(lambda a, b: a + b, [1, 2, 3]) # 6' },
        { id: 'try_except', group: 'key', label: 'try/except/finally', size: 12, desc: 'Runs code that might fail, catches specific exceptions, and optionally runs cleanup code regardless of the outcome.', parent: 'ErrorHandling', params: ['block'], example: 'try:\n    risky()\nexcept ValueError as e:\n    print(e)\nfinally:\n    cleanup()' },
        { id: 'raise_py', group: 'key', label: 'raise', size: 12, desc: 'Triggers an exception explicitly, optionally re-raising a caught one with "raise" alone.', parent: 'ErrorHandling', params: ['ExceptionInstance'], example: 'raise ValueError("Invalid input")' },
        { id: 'exception_base', group: 'key', label: 'Exception', size: 12, desc: 'The base class for most built-in errors; user-defined exceptions typically inherit from it.', parent: 'ErrorHandling', params: [], example: 'try:\n    1 / 0\nexcept Exception as e:\n    print(e)' },
        { id: 'custom_exception', group: 'key', label: 'Custom Exception', size: 12, desc: 'A user-defined error type created by subclassing Exception, useful for signaling domain-specific failures.', parent: 'ErrorHandling', params: ['message'], example: 'class InsufficientFundsError(Exception):\n    pass' },
        { id: 'import_py', group: 'key', label: 'import', size: 12, desc: 'Loads a module so its functions, classes, and variables can be used in the current file.', parent: 'Modules', params: ['module'], example: 'import math\nmath.sqrt(16)' },
        { id: 'from_import_py', group: 'key', label: 'from ... import', size: 12, desc: 'Imports specific names directly from a module, optionally renaming them with "as".', parent: 'Modules', params: ['module', 'name'], example: 'from datetime import datetime as dt' },
        { id: 'name_main_py', group: 'key', label: '__name__ == "__main__"', size: 12, desc: 'A guard that lets a file run code only when executed directly, not when imported as a module.', parent: 'Modules', params: [], example: 'if __name__ == "__main__":\n    main()' },
        { id: 'open_fn', group: 'key', label: 'open()', size: 12, desc: 'Opens a file and returns a file object, with a mode controlling read, write, or append access.', parent: 'FileIO', params: ['file', 'mode'], example: 'f = open("data.txt", "r")' },
        { id: 'read_method', group: 'key', label: '.read()', size: 12, desc: 'Reads the entire contents of an open file as a single string.', parent: 'FileIO', params: ['size'], example: 'with open("data.txt") as f:\n    content = f.read()' },
        { id: 'write_method', group: 'key', label: '.write()', size: 12, desc: 'Writes a string to an open file, returning the number of characters written.', parent: 'FileIO', params: ['text'], example: 'with open("log.txt", "a") as f:\n    f.write("done\\n")' },
        { id: 'readlines_method', group: 'key', label: '.readlines()', size: 12, desc: 'Reads all lines of a file and returns them as a list of strings.', parent: 'FileIO', params: [], example: 'with open("data.txt") as f:\n    lines = f.readlines()' },
        { id: 'iter_fn', group: 'key', label: 'iter()', size: 12, desc: 'Returns an iterator object from an iterable, which can then be advanced with next().', parent: 'Iterators', params: ['iterable'], example: 'it = iter([1, 2, 3])' },
        { id: 'next_fn', group: 'key', label: 'next()', size: 12, desc: 'Retrieves the next item from an iterator, raising StopIteration when exhausted.', parent: 'Iterators', params: ['iterator', 'default'], example: 'next(it) # 1' },
        { id: 'stopiteration_err', group: 'key', label: 'StopIteration', size: 12, desc: 'The exception automatically raised when an iterator has no more items to produce.', parent: 'Iterators', params: [], example: 'while True:\n    try:\n        val = next(it)\n    except StopIteration:\n        break' },
        { id: 'annotation_hint', group: 'key', label: 'x: int', size: 12, desc: 'Attaches an optional type hint to a variable or function parameter, checked by external tools rather than at runtime.', parent: 'TypeHints', params: [], example: 'def greet(name: str) -> str:\n    return f"Hi {name}"' },
        { id: 'optional_hint', group: 'key', label: 'Optional[T]', size: 12, desc: 'Indicates that a value can be of type T or None.', parent: 'TypeHints', params: ['T'], example: 'def find(id: int) -> Optional[User]: ...' },
        { id: 'list_generic_hint', group: 'key', label: 'list[T]', size: 12, desc: 'Annotates a list where every element is expected to be of type T.', parent: 'TypeHints', params: ['T'], example: 'def total(values: list[float]) -> float: ...' },
        { id: 'union_hint', group: 'key', label: 'Union[A, B]', size: 12, desc: 'Indicates that a value may be one of several specified types.', parent: 'TypeHints', params: ['A', 'B'], example: 'def parse(value: Union[str, int]) -> str: ...' },
        { id: 'list_comp', group: 'key', label: 'List Comprehension', size: 12, desc: 'Builds a new list in a single expressive line by transforming and optionally filtering an iterable.', parent: 'Comprehensions', params: [], example: 'squares = [x**2 for x in range(10) if x % 2 == 0]' },
        { id: 'dict_comp', group: 'key', label: 'Dict Comprehension', size: 12, desc: 'Builds a dictionary in one line from an iterable of key-value pairs.', parent: 'Comprehensions', params: [], example: 'squares = {x: x**2 for x in range(5)}' },
        { id: 'set_comp', group: 'key', label: 'Set Comprehension', size: 12, desc: 'Builds a set in one line, automatically removing duplicate results.', parent: 'Comprehensions', params: [], example: 'evens = {x for x in range(10) if x % 2 == 0}' },
        { id: 'gen_expr', group: 'key', label: 'Generator Expression', size: 12, desc: 'Like a list comprehension but lazy, producing values one at a time without building the whole list in memory.', parent: 'Comprehensions', params: [], example: 'total = sum(x**2 for x in range(1000000))' },
        { id: 'counter_std', group: 'key', label: 'Counter', size: 12, desc: 'A dict subclass from the collections module that counts hashable items automatically.', parent: 'StdLib', params: ['iterable'], example: 'from collections import Counter\nCounter("aabbc") # {"a":2, "b":2, "c":1}' },
        { id: 'defaultdict_std', group: 'key', label: 'defaultdict', size: 12, desc: 'A dict subclass that automatically creates a default value for missing keys instead of raising KeyError.', parent: 'StdLib', params: ['default_factory'], example: 'from collections import defaultdict\nd = defaultdict(list)\nd["a"].append(1)' },
        { id: 'namedtuple_std', group: 'key', label: 'namedtuple', size: 12, desc: 'Creates a lightweight, immutable tuple subclass with named fields for readable attribute access.', parent: 'StdLib', params: ['typename', 'field_names'], example: 'from collections import namedtuple\nPoint = namedtuple("Point", ["x", "y"])' },
        { id: 'itertools_chain_std', group: 'key', label: 'itertools.chain()', size: 12, desc: 'Lazily combines multiple iterables into a single continuous iterator.', parent: 'StdLib', params: ['*iterables'], example: 'from itertools import chain\nlist(chain([1, 2], [3, 4])) # [1, 2, 3, 4]' },
        { id: 'datetime_std', group: 'key', label: 'datetime', size: 12, desc: 'The standard module for representing and manipulating dates and times.', parent: 'StdLib', params: [], example: 'from datetime import datetime\nnow = datetime.now()' },
        { id: 'json_std', group: 'key', label: 'json', size: 12, desc: 'The standard module for encoding Python objects to JSON strings and decoding them back.', parent: 'StdLib', params: [], example: 'import json\ndata = json.loads(\'{"a": 1}\')' },
        { id: 're_std', group: 'key', label: 're', size: 12, desc: 'The standard module for working with regular expressions, used to search and manipulate text patterns.', parent: 'StdLib', params: [], example: 'import re\nre.findall(r"\\d+", "a1 b22")' },
        { id: 'fstring_str', group: 'key', label: 'f-string', size: 12, desc: 'A string literal prefixed with f that lets you embed expressions directly inside curly braces.', parent: 'Strings', params: [], example: 'name = "Ana"\nprint(f"Hello, {name}!")' },
        { id: 'format_method_str', group: 'key', label: '.format()', size: 12, desc: 'Substitutes placeholders in a string with formatted values, predating f-strings.', parent: 'Strings', params: ['*args', '**kwargs'], example: '"{} is {}".format("Age", 25)' },
        { id: 'split_str', group: 'key', label: '.split()', size: 12, desc: 'Breaks a string into a list of substrings based on a separator, defaulting to whitespace.', parent: 'Strings', params: ['sep', 'maxsplit'], example: '"a,b,c".split(",") # ["a", "b", "c"]' },
        { id: 'join_str', group: 'key', label: '.join()', size: 12, desc: 'Concatenates an iterable of strings using the string it is called on as the separator.', parent: 'Strings', params: ['iterable'], example: '"-".join(["a", "b", "c"]) # "a-b-c"' },
        { id: 'strip_str', group: 'key', label: '.strip()', size: 12, desc: 'Removes leading and trailing whitespace (or specified characters) from a string.', parent: 'Strings', params: ['chars'], example: '"  hi  ".strip() # "hi"' },
        { id: 'replace_str', group: 'key', label: '.replace()', size: 12, desc: 'Returns a copy of a string with all occurrences of a substring replaced by another.', parent: 'Strings', params: ['old', 'new', 'count'], example: '"hello".replace("l", "L") # "heLLo"' },
        { id: 'slicing_str', group: 'key', label: 'Slicing [a:b]', size: 12, desc: 'Extracts a portion of a sequence using start, stop, and optional step indices.', parent: 'Strings', params: ['start', 'stop', 'step'], example: '"hello"[1:4] # "ell"' },
    ],
    links: []
};