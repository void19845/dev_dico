import { LanguageData } from '../types.js';

export const typescript: LanguageData = {
    color: '#3178C6',
    nodes: [
        { id: 'TS', group: 'root', label: 'TypeScript', size: 30, desc: 'A strongly typed superset of JavaScript that adds optional static typing to the language, allowing developers to catch errors early and document code intent.' },
        { id: 'Types', group: 'cat', label: 'Types', size: 20, desc: 'The core system for defining data shapes. In TypeScript, types act as a contract that the compiler enforces during development.' },
        { id: 'Generics', group: 'cat', label: 'Generics', size: 20, desc: 'A tool for creating reusable components that work with a variety of types rather than a single one.' },
        { id: 'Utility', group: 'cat', label: 'Utility Types', size: 20, desc: 'Built-in global types that allow you to transform an existing type into a new one without manual re-definition.' },
        { id: 'Async', group: 'cat', label: 'Asynchrony', size: 20, desc: 'Patterns for handling non-blocking operations, ensuring the main thread remains responsive.' },
        { id: 'Control', group: 'cat', label: 'Control Flow', size: 20, desc: 'Logic for narrowing types and directing program execution based on state.' },
        { id: 'Advanced', group: 'cat', label: 'Advanced Types', size: 20, desc: 'Powerful type-level programming tools for building highly flexible and dynamic type systems.' },
        { id: 'type', group: 'key', label: 'type', size: 12, desc: 'Defines a type alias. It is used to give a new name to a type, which can be a primitive, union, or intersection.', parent: 'Types', params: ['AliasName', 'TypeDefinition'], example: 'type UserID = string | number;', tip: 'Use type aliases for unions and intersections, as interfaces cannot do this directly.' },
        { id: 'interface', group: 'key', label: 'interface', size: 12, desc: 'Defines the structure of an object. Unlike type aliases, interfaces are extendable and ideal for defining APIs or public contracts.', parent: 'Types', params: ['InterfaceName', 'Members'], example: 'interface User { name: string; age: number; }' },
        { id: 'enum', group: 'key', label: 'enum', size: 12, desc: 'A way to define a set of named constants. Enums improve readability by replacing magic numbers or strings with semantic names.', parent: 'Types', params: ['EnumName', 'Members'], example: 'enum Role { Admin, User, Guest }' },
        { id: 'unknown', group: 'key', label: 'unknown', size: 12, desc: 'The safest version of "any". It tells TypeScript that you don\'t know the type yet, and you must perform a type check before using it.', parent: 'Types', params: [], example: 'let value: unknown = "Hello";', tip: 'Always prefer unknown over any when you truly don\'t know the type; it forces you to handle the data safely.' },
        { id: 'never', group: 'key', label: 'never', size: 12, desc: 'Represents a value that should never exist, such as a function that always throws an error or an infinite loop.', parent: 'Types', params: [], example: 'function throwError(): never { throw new Error(); }' },
        { id: 'void', group: 'key', label: 'void', size: 12, desc: 'Used as the return type of functions that do not return a value. It indicates the function performs an action but returns nothing.', parent: 'Types', params: [], example: 'function log(msg: string): void { console.log(msg); }' },
        { id: 'T', group: 'key', label: '<T>', size: 12, desc: 'A type parameter used in generics. It allows a function or class to work with any type provided by the caller.', parent: 'Generics', params: ['T: Constraint'], example: 'function wrap<T>(item: T): T[] { return [item]; }' },
        { id: 'K', group: 'key', label: '<K>', size: 12, desc: 'Typically used in generics to represent a Key. Often constrained using "extends keyof T" to ensure the key exists on a specific object.', parent: 'Generics', params: ['K extends keyof T'], example: 'function getProp<T, K extends keyof T>(obj: T, key: K) { return obj[key]; }' },
        { id: 'Partial', group: 'key', label: 'Partial<T>', size: 12, desc: 'Creates a type where every property of T is made optional. Perfect for "update" functions where only some fields are changed.', parent: 'Utility', params: ['T'], example: 'type UpdateUser = Partial<User>;' },
        { id: 'Readonly', group: 'key', label: 'Readonly<T>', size: 12, desc: 'Makes all properties of T immutable. Any attempt to change a property after creation will cause a compile-time error.', parent: 'Utility', params: ['T'], example: 'const config: Readonly<Config> = { port: 8080 };' },
        { id: 'Pick', group: 'key', label: 'Pick<T, K>', size: 12, desc: 'Creates a new type by selecting a specific subset of properties K from type T.', parent: 'Utility', params: ['T', 'K'], example: 'type UserPreview = Pick<User, "name" | "avatar">;' },
        { id: 'Omit', group: 'key', label: 'Omit<T, K>', size: 12, desc: 'The opposite of Pick. It removes specified properties K from type T, keeping everything else.', parent: 'Utility', params: ['T', 'K'], example: 'type UserWithoutPassword = Omit<User, "password">;' },
        { id: 'Record', group: 'key', label: 'Record<K, T>', size: 12, desc: 'Constructs an object type where keys are of type K and values are of type T. Ideal for maps or dictionaries.', parent: 'Utility', params: ['K', 'T'], example: 'const users: Record<string, User> = { "1": user1 };' },
        { id: 'async', group: 'key', label: 'async', size: 12, desc: 'Marks a function as asynchronous, meaning it will always return a Promise and allows the use of the "await" keyword inside.', parent: 'Async', params: ['function'], example: 'async function fetchData() { ... }' },
        { id: 'await', group: 'key', label: 'await', size: 12, desc: 'Pauses the execution of an async function until a Promise is resolved, returning the resolved value.', parent: 'Async', params: ['Promise'], example: 'const data = await response.json();' },
        { id: 'Promise', group: 'key', label: 'Promise<T>', size: 12, desc: 'An object representing the eventual completion (or failure) of an asynchronous operation and its resulting value.', parent: 'Async', params: ['T'], example: 'const p: Promise<string> = fetchUser();' },
        { id: 'as', group: 'key', label: 'as', size: 12, desc: 'A type assertion that tells the compiler "trust me, I know the type is actually X". Use with caution as it bypasses some checks.', parent: 'Control', params: ['Type'], example: 'const input = document.getElementById("in") as HTMLInputElement;' },
        { id: 'is', group: 'key', label: 'is', size: 12, desc: 'A type predicate used in functions to tell TypeScript that if the function returns true, the variable is of a specific type.', parent: 'Control', params: ['Type'], example: 'function isUser(obj: any): obj is User { return "name" in obj; }' },
        { id: 'cond', group: 'key', label: 'Conditional Types', size: 12, desc: 'Logic that allows a type to change based on a condition, similar to a ternary operator for types.', parent: 'Advanced', params: ['T extends U ? X : Y'], example: 'type IsString<T> = T extends string ? true : false;' },
        { id: 'mapped', group: 'key', label: 'Mapped Types', size: 12, desc: 'Allows you to create a new type by iterating over the keys of an existing type and transforming them.', parent: 'Advanced', params: ['[K in keyof T]: Value'], example: 'type Options<T> = { [K in keyof T]: boolean };' },
        { id: 'template', group: 'key', label: 'Template Literals', size: 12, desc: 'Combines string literals with other types to create highly specific string types.', parent: 'Advanced', params: ['`${T}`'], example: 'type Greeting = `Hello ${string}`;' },
        { id: 'Classes', group: 'cat', label: 'Classes', size: 20, desc: 'Object-oriented building blocks that add access modifiers and static typing on top of JavaScript classes.' },
        { id: 'Operators', group: 'cat', label: 'Operators', size: 20, desc: 'Syntax shortcuts for safely accessing, combining, and asserting the shape of values.' },
        { id: 'ModulesTS', group: 'cat', label: 'Modules', size: 20, desc: 'The system for splitting code across files and controlling what each file exposes.' },
        { id: 'Functions', group: 'cat', label: 'Functions', size: 20, desc: 'Typed function signatures, including parameter defaults, rest parameters, and multiple overloads.' },
        { id: 'TupleUnion', group: 'cat', label: 'Composite Types', size: 20, desc: 'Ways of combining existing types into tuples, unions, intersections, and literal values.' },
        { id: 'Exclude', group: 'key', label: 'Exclude<T, U>', size: 12, desc: 'Constructs a type by removing from T all members that are assignable to U.', parent: 'Utility', params: ['T', 'U'], example: 'type T = Exclude<"a" | "b" | "c", "a">; // "b" | "c"' },
        { id: 'Extract', group: 'key', label: 'Extract<T, U>', size: 12, desc: 'Constructs a type by keeping only the members of T that are assignable to U.', parent: 'Utility', params: ['T', 'U'], example: 'type T = Extract<"a" | "b" | "c", "a" | "f">; // "a"' },
        { id: 'NonNullable', group: 'key', label: 'NonNullable<T>', size: 12, desc: 'Removes null and undefined from a type, leaving only the values that can actually occur.', parent: 'Utility', params: ['T'], example: 'type T = NonNullable<string | null>; // string' },
        { id: 'ReturnType', group: 'key', label: 'ReturnType<T>', size: 12, desc: 'Extracts the return type of a function type as its own reusable type.', parent: 'Utility', params: ['T'], example: 'function f() { return { id: 1 }; }\ntype R = ReturnType<typeof f>;' },
        { id: 'Parameters', group: 'key', label: 'Parameters<T>', size: 12, desc: 'Extracts the parameter types of a function type as a tuple.', parent: 'Utility', params: ['T'], example: 'type P = Parameters<(a: string, b: number) => void>; // [string, number]' },
        { id: 'Required', group: 'key', label: 'Required<T>', size: 12, desc: 'Makes every optional property of T mandatory, the opposite of Partial.', parent: 'Utility', params: ['T'], example: 'type StrictUser = Required<Partial<User>>;' },
        { id: 'Awaited', group: 'key', label: 'Awaited<T>', size: 12, desc: 'Unwraps the type inside a Promise, recursively, mirroring what "await" would resolve to.', parent: 'Utility', params: ['T'], example: 'type T = Awaited<Promise<Promise<number>>>; // number' },
        { id: 'InstanceType', group: 'key', label: 'InstanceType<T>', size: 12, desc: 'Extracts the instance type produced by a class constructor type.', parent: 'Utility', params: ['T'], example: 'class Point {}\ntype P = InstanceType<typeof Point>;' },
        { id: 'class_ts', group: 'key', label: 'class', size: 12, desc: 'Defines a blueprint for objects, combining typed fields, methods, and constructors.', parent: 'Classes', params: ['ClassName'], example: 'class User {\n  constructor(public name: string) {}\n}' },
        { id: 'extends_ts', group: 'key', label: 'extends', size: 12, desc: 'Establishes inheritance between a subclass and its base class, or between two interfaces.', parent: 'Classes', params: ['BaseClass'], example: 'class Admin extends User { ... }' },
        { id: 'implements_ts', group: 'key', label: 'implements', size: 12, desc: 'Declares that a class must satisfy the structure defined by one or more interfaces.', parent: 'Classes', params: ['Interface'], example: 'class Car implements Drivable { drive() {} }' },
        { id: 'access_modifiers_ts', group: 'key', label: 'public/private/protected', size: 12, desc: 'Controls the visibility of class members: public everywhere, private only within the class, protected within the class and subclasses.', parent: 'Classes', params: [], example: 'class Account {\n  private balance: number = 0;\n}' },
        { id: 'readonly_ts', group: 'key', label: 'readonly', size: 12, desc: 'Marks a property so it can only be assigned once, either at declaration or in the constructor.', parent: 'Classes', params: [], example: 'class Point {\n  readonly x: number;\n}' },
        { id: 'abstract_ts', group: 'key', label: 'abstract class', size: 12, desc: 'A class that cannot be instantiated directly and may declare methods without implementations for subclasses to provide.', parent: 'Classes', params: [], example: 'abstract class Shape {\n  abstract area(): number;\n}' },
        { id: 'static_ts', group: 'key', label: 'static', size: 12, desc: 'Declares a member that belongs to the class itself rather than to individual instances.', parent: 'Classes', params: [], example: 'class MathUtils {\n  static PI = 3.14;\n}' },
        { id: 'constructor_ts', group: 'key', label: 'constructor', size: 12, desc: 'A special method invoked when a class is instantiated with "new", used to initialize fields.', parent: 'Classes', params: ['args'], example: 'constructor(public name: string, private age: number) {}' },
        { id: 'getter_setter_ts', group: 'key', label: 'get/set', size: 12, desc: 'Accessor methods that let a property be computed on read or validated on write, while looking like plain fields.', parent: 'Classes', params: [], example: 'class Circle {\n  get area() { return Math.PI * this.r ** 2; }\n}' },
        { id: 'optional_chaining', group: 'key', label: '?.', size: 12, desc: 'Safely accesses a nested property, short-circuiting to undefined instead of throwing if a reference is null or undefined.', parent: 'Operators', params: [], example: 'const city = user?.address?.city;' },
        { id: 'nullish_coalescing', group: 'key', label: '??', size: 12, desc: 'Returns the right-hand value only when the left-hand value is null or undefined, unlike || which also triggers on falsy values.', parent: 'Operators', params: [], example: 'const port = config.port ?? 3000;' },
        { id: 'spread_ts', group: 'key', label: '... (spread)', size: 12, desc: 'Expands an array or object into individual elements or properties, often used to copy or merge values.', parent: 'Operators', params: [], example: 'const merged = { ...defaults, ...overrides };' },
        { id: 'destructuring_ts', group: 'key', label: 'Destructuring', size: 12, desc: 'Unpacks values from arrays or properties from objects into distinct typed variables.', parent: 'Operators', params: [], example: 'const { name, age }: User = getUser();' },
        { id: 'non_null_assertion', group: 'key', label: '! (non-null assertion)', size: 12, desc: 'Tells the compiler to treat a value as non-null and non-undefined, bypassing its own narrowing.', parent: 'Operators', params: [], example: 'const el = document.getElementById("app")!;' },
        { id: 'satisfies_ts', group: 'key', label: 'satisfies', size: 12, desc: 'Checks that an expression matches a type without widening or changing the expression\'s inferred type.', parent: 'Operators', params: ['Type'], example: 'const config = { port: 3000 } satisfies Config;' },
        { id: 'as_const_ts', group: 'key', label: 'as const', size: 12, desc: 'Infers the narrowest possible literal types for a value and makes its properties readonly.', parent: 'Operators', params: [], example: 'const colors = ["red", "green"] as const;' },
        { id: 'import_ts', group: 'key', label: 'import', size: 12, desc: 'Brings exported values, types, or default exports from another module into the current file.', parent: 'ModulesTS', params: ['module'], example: 'import { User } from "./types";' },
        { id: 'export_ts', group: 'key', label: 'export', size: 12, desc: 'Makes a variable, function, class, or type available for other modules to import.', parent: 'ModulesTS', params: [], example: 'export interface User { name: string; }' },
        { id: 'namespace_ts', group: 'key', label: 'namespace', size: 12, desc: 'Groups related code under a single name to avoid global scope pollution, mostly superseded by ES modules.', parent: 'ModulesTS', params: ['name'], example: 'namespace Validation {\n  export function isValid(s: string) { return s.length > 0; }\n}' },
        { id: 'overload_ts', group: 'key', label: 'Function Overloads', size: 12, desc: 'Declares multiple call signatures for the same function name, each accepting different parameter types.', parent: 'Functions', params: [], example: 'function combine(a: string, b: string): string;\nfunction combine(a: number, b: number): number;' },
        { id: 'default_params_ts', group: 'key', label: 'Default Parameters', size: 12, desc: 'Assigns a fallback value to a parameter that is used when the caller omits an argument.', parent: 'Functions', params: [], example: 'function greet(name: string = "World") { ... }' },
        { id: 'rest_params_ts', group: 'key', label: 'Rest Parameters', size: 12, desc: 'Collects any number of trailing arguments into a single typed array parameter.', parent: 'Functions', params: [], example: 'function sum(...nums: number[]): number { ... }' },
        { id: 'arrow_function_ts', group: 'key', label: 'Arrow Function', size: 12, desc: 'A concise function syntax that also lexically binds "this" to its surrounding context.', parent: 'Functions', params: [], example: 'const add = (a: number, b: number): number => a + b;' },
        { id: 'tuple_ts', group: 'key', label: 'Tuple', size: 12, desc: 'A fixed-length array where each position has its own known, potentially different type.', parent: 'TupleUnion', params: [], example: 'let point: [number, number] = [10, 20];' },
        { id: 'union_ts', group: 'key', label: 'Union (A | B)', size: 12, desc: 'Describes a value that can be one of several types, requiring narrowing before type-specific operations.', parent: 'TupleUnion', params: [], example: 'let id: string | number;' },
        { id: 'intersection_ts', group: 'key', label: 'Intersection (A & B)', size: 12, desc: 'Combines multiple types into one that must satisfy all of them simultaneously.', parent: 'TupleUnion', params: [], example: 'type Employee = Person & { salary: number };' },
        { id: 'literal_type_ts', group: 'key', label: 'Literal Type', size: 12, desc: 'Narrows a type to one exact value, such as a specific string or number, rather than the general type.', parent: 'TupleUnion', params: [], example: 'type Direction = "up" | "down" | "left" | "right";' },
        { id: 'keyof_ts', group: 'key', label: 'keyof', size: 12, desc: 'Produces a union of the property names (keys) of a given type.', parent: 'Advanced', params: ['T'], example: 'type UserKeys = keyof User; // "name" | "age"' },
        { id: 'typeof_ts', group: 'key', label: 'typeof (type query)', size: 12, desc: 'Extracts the static type of a variable or value, useful for deriving types from runtime constants.', parent: 'Advanced', params: ['value'], example: 'const config = { port: 3000 };\ntype Config = typeof config;' },
        { id: 'indexed_access_ts', group: 'key', label: 'Indexed Access Type', size: 12, desc: 'Looks up the type of a specific property within another type, similar to accessing a value at runtime.', parent: 'Advanced', params: ['T[K]'], example: 'type NameType = User["name"]; // string' },
    ],
    links: []
};